Event.setRandomNumber = SELECT trunc(random() * 9999 + 1000) FROM generate_series(1,1);
Event.listConferences= SELECT  a.nombres, m.assisted, c.tittle FROM conference c JOIN event m ON c.id = m.conference_id join assistant on a.id=m.assistant_id where m.assisted=true